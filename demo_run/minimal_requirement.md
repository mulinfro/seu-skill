# 最小演示需求

实现一个极简命令行计算器 `calc.py`：
- 支持 `add` 与 `sub` 两个子命令。
- 输入为两个整数，例如：`python demo_run/src/calc.py add 2 3`。
- 正常输出结果数字；输入非法时输出错误提示并返回非 0 退出码。
- 补充基础单元测试，覆盖正常路径与错误路径。

验收标准：
1. `add 2 3` 输出 `5`
2. `sub 5 2` 输出 `3`
3. 非法命令如 `mul 2 3` 返回非 0
4. 单测可运行通过
